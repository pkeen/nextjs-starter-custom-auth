import React from "react";

type Props = {};

const page = (props: Props) => {
	return (
		<div>
			<h1>Dashboard</h1>
			<p>Not available to non-signed in users</p>
		</div>
	);
};

export default page;
